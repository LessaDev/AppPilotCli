import {StyleSheet} from 'react-native';

export default () =>
  StyleSheet.create({
    container: {
      backgroundColor: '#F2F2F2',
      flexGrow: 1,
      alignItems: 'center',
    },
    extractlistItens: {
      flex: 1,
      width: '100%',
      marginLeft: 55,
    },
  });
